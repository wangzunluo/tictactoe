def checkHorizontal():

def checkVertical():

def checkDiagonal():

def checkFull(board):
  for row in board:
    for cell in row:
      if not board[row][cell]
        return False
  return True

def check
def checkStatus(board, user, x, y):
  [0,1], [1,0], [1,2], [2,1]
  isEdge = checkEdge(board, x, y):

  full = checkFull(board)